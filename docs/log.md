# Work Log

Format — one line per entry:
`YYYY-MM-DD | Task N | <one sentence describing what was done`

---

2026-04-02 | Setup | Created docs/todo.md, docs/prompt.md, and docs/log.md to plan the React 19 migration.
2026-04-02 | Task 1 | Scaffolded React 19 + Vite SWC + TypeScript + Tailwind CSS v4 project in app/, ported music theory logic to src/lib/musicScale.ts, and added Home/Kirkkosavellajit/Harjoittelu screens with state-based routing and mobile-first MobileShell layout.
2026-04-02 | Task 2 | Created six reusable TypeScript components in src/components/ui/: Button (key/mode variants), MobileShell, ScreenHeader, SectionCard, ModeChip, and HomeCard, all using Tailwind utility classes with the medieval parchment aesthetic.
2026-04-03 | Task 3 | Migrated all screens to use UI components from Task 2, added Zustand store with persist middleware for key/mode state, wired lazy loading with Suspense in App.tsx, and removed legacy vanilla JS files (script.js, events.js, index.html, styles.css).
2026-04-04 | Task 4 | Configured vite.config.ts to output build to repo-root dist/, added deploy:build script in app/package.json, updated CI workflow to deploy from dist/ with per-extension MIME types (no build step in CI), and created docs/deployment.md.
2026-04-04 | Task 5 | Redesigned HomeCard with solid gradient backgrounds, inline SVG icons (book-with-note and violin), subtitle prop, depth shadows, and active:scale-95 tap feedback; updated Home.tsx to pass new icons and subtitles.
2026-04-04 | Task 6 | Replaced ← text character in ScreenHeader with an inline SVG chevron-left icon, set 44×44px touch target on the back button, left-aligned the title, and added min-h-[52px] to keep header height in the 48–56px range.
2026-04-04 | Task 7 | Reduced canvas left/right staff margins (80→5, w-30→w-5), enlarged treble clef (105→126px, shifted 10px up, x adjusted from 100 to 25), and updated note layout (startX 190→115, spacing 100→123) to fill horizontal space in mobile portrait.
2026-04-04 | Task 8 | Added desktop view mode: created useViewport hook (769px breakpoint), App.tsx conditionally skips MobileShell on desktop, Kirkkosavellajit renders two-column layout with always-visible sidebar controls and centered 1000×500 canvas, Home shows side-by-side cards, and accidental font sizes adapt to viewport.
2026-04-04 | Task 9 | Created docs/scale-practice-method.md with structured violin scale practice methodology: 3 skill levels (10/12/14 scales), circle-of-fifths key progression based on Flesch/Galamian, shift exercises for 1st–3rd position, arpeggio patterns per level, and implementation-ready structured data tables.
2026-04-04 | Task 10 | Built Harjoittelu info page in Finnish: replaced placeholder screen with 8 collapsible accordion sections (Harjoitusmenetelmä, Taso 1/2/3 scale tables, Asemat, Siirtymisharjoitukset, Arpeggiot, Lähteet); created AccordionSection component (mobile: collapsible, desktop: always-open); set up Vitest with jsdom and added smoke test.
2026-04-06 | Task 11 | Built interactive scale practice routine: created practiceMethod.ts (36 scales across 3 levels with shuffle/format helpers), practiceStore.ts (Zustand + persist for session state), and added Tietoa/Harjoittele tab navigation to Harjoittelu screen with level selector, randomised checklist, progress bar, completion screen with repeat/reshuffle options.
2026-04-06 | Task 12 | Added scale info panel to practice routine: extended practiceMethod.ts with getScaleDetail() using musicScale.ts for note generation, created ScaleDetailPanel (notes/shift/routine/arpeggio display) and ScaleDetailModal (fullscreen mobile overlay with browser-back support), added info button to each practice item, desktop two-column layout (list + sticky side panel, max-w 900px), updated ux-spec and ui-components docs.
2026-04-06 | Task 13 | Extracted canvas drawing into reusable library: created musicStave.ts (pure TS drawing functions with parameterised geometry), MusicCanvas.tsx component (wraps canvas + useEffect), refactored Kirkkosavellajit.tsx to use MusicCanvas (removed ~100 lines of inline drawing code), updated architecture.md with new library docs.
2026-04-06 | Task 14 | Tightened practice session state and layout hygiene: added clearSession() action to practiceStore (replaces direct setState calls from screen), replaced array-index React keys with stable getScaleKey() based on scale entry properties, removed all inline style attributes from Kirkkosavellajit (replaced with Tailwind classes).
2026-04-13 | Task 15 | Replaced Nuotit text with compact MusicCanvas in ScaleDetailPanel: added scaleKey/scaleMode to ScaleDetail interface, added compact flag to computeLayout (tighter clef/note geometry for small canvases), rendered 260×130 single-stave canvas with note names as helper text below.
2026-04-13 | Task 16 | Added octave-aware note system: created noteOctave.ts with NoteWithOctave type, Finnish/Helmholtz/SPN formatting for all octaves 0–8, getAbsoluteNoteY() for absolute staff positioning; added buildArpeggioNotesWithOctave() to practiceMethod.ts, renderArpeggio()/drawNoteAt() to musicStave.ts, arpeggioNotes prop to MusicCanvas; added 23 unit tests covering Y-position calculations, Finnish note names, Helmholtz notation, and arpeggio generation.
2026-04-13 | Task 17 | Replaced text-only arpeggio display in ScaleDetailPanel with compact MusicCanvas: added arpeggioNotesWithOctave to ScaleDetail interface, populated via buildArpeggioNotesWithOctave() in getScaleDetail(), rendered 260×130 single-stave arpeggio canvas with note names and description as helper text below.
2026-05-15 | Task 18 | Migrated scale rendering to octave-aware absolute positioning: deleted NOTE_TO_STAFF_POSITION/getNoteY/calculateStaffSteps/getNoteInfo from musicScale.ts, added SCALE_START_OCTAVE + assignAscendingOctaves + isInDrawingRange to noteOctave.ts, rewrote renderScale and drawNoteAt in musicStave.ts to use getAbsoluteNoteY with hard [G3, G6] drawing bound (out-of-range notes log console.error and skip), aligned arpeggio start octave with scale start octave so the roots share a staff line, and added 14 new tests covering range checks, ascending-octave assignment, and scale/arpeggio root alignment.
2026-05-15 | Task 19 | Rendered canvas at on-screen pixel size: MusicCanvas now uses ResizeObserver to measure its wrapper, sets bitmap to cssSize × devicePixelRatio with ctx.setTransform(dpr,...), and drops the width/height/mobile/compact props (callers give the wrapper a sized class like w-full aspect-[2/1]); musicStave.computeLayout derives lineSpacing, staff Y positions, clef/accidental sizes, note-head/stem geometry, ledger width, and note spacing proportionally from the measured CSS size — no more fixed 1000×500 constants; drawStaffLines and drawLedgerLines snap to Math.round(y)+0.5 so 1px strokes stay crisp at any DPR.
2026-05-15 | Task 20 | Built Soittohetki screen: lazy-loaded route `/soittohetki?root&mode&octaves&min`, scale/arpeggio toggle reusing MusicCanvas, drift-free useCountdownTimer hook (~50 lines in lib/), numeric duration chips (1/3/5/10) and play/pause/reset icon buttons on one row, animation placeholder reserved for Task 21; added a stick-figure entry button next to the info button on each Harjoittelu practice row.
2026-05-16 | Task 21 | Built procedural pelican timer animation: shared SVG rig in app/src/components/animations/PelicanTimer.tsx with two variants (walking — waddling across a scrolling ground strip; flying — gliding on an arc with drifting clouds and a rising sun), driven by `--duration` CSS var so `animation-duration` matches durationMs, paused via `is-paused`/`animation-play-state`, rewound by remounting on a `runId`+`durationMs` key; added keyframes and anatomical `transform-origin` pivots to app/src/index.css, replaced the Soittohetki placeholder with `<PelicanTimer>` (variant via `?anim=walking|flying`), and added a hidden DEBUG route `#/dev/animation/timer` (AnimationTest screen) with variant/duration/pause/restart controls.
2026-05-16 | Task 22 | Built procedural pelican time-up celebration in app/src/components/animations/PelicanCelebration.tsx with two variants (walking — proud bounce + falling confetti + pulsing sun; flying — glide → dive → splash burst → pouch inflation over a sea/sky gradient), reusing the WalkingPelicanSvg/FlyingPelicanSvg rigs (now exported from PelicanTimer.tsx) and driven by a `--celebration-duration` CSS var; added the keyframes and a `prefers-reduced-motion` static `Valmis!` fallback to app/src/index.css; wired Soittohetki to set `showCelebration` from `useCountdownTimer`'s onComplete and swap `<PelicanTimer>` for `<PelicanCelebration>` (cleared on Start/Reset/duration change); registered a hidden DEBUG route `#/dev/animation/celebration` (CelebrationAnimationTest screen) with variant/duration/replay controls.
